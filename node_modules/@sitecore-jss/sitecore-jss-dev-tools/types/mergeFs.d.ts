export interface FileResult {
    filename: string;
    path: string;
    contents: any;
}
export type CustomFileParser = (filePath: string, contents: string) => unknown | null;
export interface MergeFsResult {
    path: string;
    name: string;
    files: FileResult[];
    folders: MergeFsResult[];
}
export declare const mergeFs: (rootPath: string, parseFileContents?: CustomFileParser) => Promise<MergeFsResult>;
